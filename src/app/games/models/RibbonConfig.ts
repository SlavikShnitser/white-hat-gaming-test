/**
 * Defines configuration object of the ribbon.
 */
export interface RibbonConfig {
  /** Class name to be added to the ribbon container. */
  className: string;

  /** Path to ribbon asset. */
  image: string;

  /** Text to display over the ribbon. */
  label: string;
}
